import React from 'react';
import { render } from '@testing-libary/react';
import Episodes from './Episodes';

test('Renders main image from the API', () => {
    const {} = render(
        <Episodes episodes={{}}/>
    )
})